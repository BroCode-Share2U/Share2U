{% extends "layout.html.twig" %}

{% block title '' %}

{% block content %}

    {{ dump(user) }}
    <br>
    {{ dump(items) }}
    <br>
    {{ dump(requestsIn) }}
    <br>
    {{ dump(requestsOut) }}
    <br>
    {{ dump(loansIn) }}
    <br>
    {{ dump(loansOut) }}
    <br>
    {{ dump(closedLoansIn) }}
    <br>
    {{ dump(closedLoansOut) }}

{% endblock %}